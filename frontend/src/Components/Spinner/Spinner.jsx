import React from "react";
import "./Spinner.css";

const Spinner = () => {
  return (
    <div className="spinnerWrapper">
      <span className="loader"></span>
    </div>
  );
};

export default Spinner;